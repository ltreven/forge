FROM ghcr.io/openclaw/openclaw:latest

# We'll stick to the base image's configuration to ensure the application starts correctly.
# The base image already has the WORKDIR and application files set up.
