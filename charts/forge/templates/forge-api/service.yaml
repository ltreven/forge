{{- if .Values.forgeApi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: forge-api
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "forge.labels" . | nindent 4 }}
    {{- include "forge.forgeApi.selectorLabels" . | nindent 4 }}
spec:
  # ClusterIP only — the API is never exposed externally.
  # Access from outside the cluster is blocked by design (no Ingress + NetworkPolicy).
  type: {{ .Values.forgeApi.service.type }}
  ports:
    - port: {{ .Values.forgeApi.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "forge.forgeApi.selectorLabels" . | nindent 4 }}
{{- end }}
